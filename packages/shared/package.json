{
  "name": "@least-count/shared",
  "version": "1.0.0",
  "description": "Shared TypeScript types for Least Count game",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "typescript": "^5.3.2",
    "rimraf": "^5.0.5"
  },
  "files": [
    "dist"
  ]
}
