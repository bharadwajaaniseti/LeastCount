{
  "name": "least-count",
  "version": "1.0.0",
  "description": "Least Count card game with React frontend and Socket.io backend",
  "scripts": {
    "dev": "concurrently \"pnpm run dev:server\" \"pnpm run dev:client\"",
    "dev:client": "cd apps/client && pnpm dev",
    "dev:server": "cd apps/server && pnpm dev",
    "build": "pnpm run build:shared && pnpm run build:client && pnpm run build:server",
    "build:shared": "cd packages/shared && pnpm build",
    "build:client": "cd apps/client && pnpm build",
    "build:server": "cd apps/server && pnpm build",
    "lint": "pnpm run lint:client && pnpm run lint:server",
    "lint:client": "cd apps/client && pnpm lint",
    "lint:server": "cd apps/server && pnpm lint",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md}\"",
    "install:all": "pnpm install && cd apps/client && pnpm install && cd ../server && pnpm install && cd ../../packages/shared && pnpm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "prettier": "^3.1.0",
    "@typescript-eslint/eslint-plugin": "^6.13.1",
    "@typescript-eslint/parser": "^6.13.1",
    "eslint": "^8.54.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
