{
  "name": "least-count",
  "version": "1.0.0",
  "description": "Least Count card game with React frontend and Socket.io backend",
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:client": "cd apps/client && npm run dev",
    "dev:server": "cd apps/server && npm run dev",
    "build": "npm run build:shared && npm run build:client && npm run build:server",
    "build:shared": "cd packages/shared && npm run build",
    "build:client": "cd apps/client && npm run build",
    "build:server": "cd apps/server && npm run build",
    "lint": "npm run lint:client && npm run lint:server",
    "lint:client": "cd apps/client && npm run lint",
    "lint:server": "cd apps/server && npm run lint",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md}\"",
    "install:all": "npm install && cd apps/client && npm install && cd ../server && npm install && cd ../../packages/shared && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "prettier": "^3.1.0",
    "@typescript-eslint/eslint-plugin": "^6.13.1",
    "@typescript-eslint/parser": "^6.13.1",
    "eslint": "^8.54.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  }
}
